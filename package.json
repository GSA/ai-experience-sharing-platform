{
  "name": "ai-experience-sharing-platform",
  "private": true,
  "version": "0.1.1",
  "description": "A CMS that has public and private content, initially focused on AI Use Case sharing.",
  "scripts": {
    "build": "cd ui && npm install --global yarn && yarn && yarn build && cd ../cms && cp -R ../ui/build/* ./public/ && npm i && npm run build",
    "start": "cd cms && npm run strapi"
  },
  "devDependencies": {
  },
  "dependencies": {
  },
  "resolutions": {
  },
  "author": {
    "name": "A Strapi developer"
  },
  "license": "MIT"
}
